<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="XamarinSample.Views.Texting">
    <ContentPage.Content>
        <StackLayout>
            <Frame BackgroundColor="#2196F3" Padding="24" CornerRadius="0">
                <Label Text="Texting Samples" HorizontalTextAlignment="Center" TextColor="White" FontSize="36"/>
            </Frame>

            <Button Text="Send Emergency Text"
                    VerticalOptions="StartAndExpand"
                    HorizontalOptions="Center"
                    Clicked="onSOSButtonClicked" />

            <Button Text="Back to Front Page"
                    VerticalOptions="EndAndExpand"
                    HorizontalOptions="Center"
                    Clicked="onBackButtonClicked" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>